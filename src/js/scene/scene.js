/* jshint esversion: 6 */

// import { randomColor } from '../colors/colors';

export default class Scene extends THREE.Scene {

	constructor() {
		super();
		// this.background = new THREE.Color(randomColor());
		// scene.fog = new THREE.Fog(scene.background, 0, 50);
	}

}
